#include <iostream>

int main() {
  // 1)
  // create a list of integers
  // loop through it, printing out each item

  // 2)
  // loop through it, printing out each item doubled

  // 3)
  // loop through it, printing out only items less than 5
}
